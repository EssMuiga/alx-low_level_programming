#include <stdio.h>
/**
 * main  prin single digit numbers of base 10 fom 0)
 * Return: 0 if successful
 */
int main(void)
{
	int i;
	for (i = 0; i <= 9; i++)
	{
		putchar(i);
	}
	putchar('\n');
	return (0);
}
