#include <stdio.h>
/**
 * _strcat - function that concatenates two strings
 * @dest: the string that is to be appended to
 * @src: the string to be added
 * return: the reulting dest
 */
char *_strcat(char *dest, char *src)
{
	int i, j;

	i = 0;
	j = 0;
	for (i = 0; dest[i] != '\0'; i++)
	{
	}
	for (j = 0; (dest[i + j] = *src++) != '\0'; j++)
	{
	}
	return (dest);
}
